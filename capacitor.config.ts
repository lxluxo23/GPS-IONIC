import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lxdeveloper.gps',
  appName: 'gps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
